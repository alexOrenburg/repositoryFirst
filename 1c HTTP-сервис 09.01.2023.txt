
Функция ШаблонURL1GET(Запрос) 
	Param=Запрос.ПараметрыЗапроса;
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type","application/json");
	ТелоJSON=ПолучитьДанныеДляОтветаВjson();
	Ответ.УстановитьТелоИзСтроки(ТелоJSON);
	Возврат Ответ;
	
КонецФункции

Функция ШаблонURL1POST(Запрос)
	
	ФорматТела=Запрос.Заголовки["Content-Type"]; 
	Если ФорматТела="application/json"  Тогда
	//Парсим json
	ИначеЕсли ФорматТела="application/xml"  Тогда
	//Парсим xml
	КонецЕсли;
	//"application/json"
	//"application/xml"
	//"text/plain"
	//...
	
	Param=Запрос.ПараметрыURL["param"]; 
	Param2=Запрос.ПараметрыЗапроса["param2"]; 
	body1=Запрос.ПолучитьТелоКакСтроку();
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type","application/json");
	ТелоJSON=ПолучитьДанныеДляОтветаВjson();
	Ответ.УстановитьТелоИзСтроки(ТелоJSON);
	Возврат Ответ;
КонецФункции

Функция ПолучитьДанныеДляОтветаВjson()
		Запись=Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	
	Запись.ЗаписатьНачалоОбъекта();
		Запись.ЗаписатьИмяСвойства("Экономист");
		
		Запись.ЗаписатьНачалоОбъекта();
		Запись.ЗаписатьИмяСвойства("Фамилия");
		Запись.ЗаписатьЗначение("Иванов");
		Запись.ЗаписатьИмяСвойства("ДатаРождения");
		Запись.ЗаписатьЗначение("19.05.1979");
		Запись.ЗаписатьКонецОбъекта();
		
		Запись.ЗаписатьИмяСвойства("Инженер");	
		
		Запись.ЗаписатьНачалоОбъекта();
		Запись.ЗаписатьИмяСвойства("Фамилия");
		Запись.ЗаписатьЗначение("Петров");
		Запись.ЗаписатьИмяСвойства("ДатаРождения");
		Запись.ЗаписатьЗначение("19.05.1971");
		Запись.ЗаписатьКонецОбъекта();
		
	Запись.ЗаписатьКонецОбъекта();	
	
	СтрокаJSON=Запись.Закрыть();

	Возврат СтрокаJSON;
	КонецФункции
